"""
回测专用配置
- 放宽筛选条件，适合历史数据回测
- 确保每次能筛选出足够的股票用于统计分析
"""

# 回测筛选参数（与实盘一致，仅成交额放宽）
BACKTEST_FILTER_CONFIG = {
    'max_pe_ratio': 30,          # PE上限：30（与实盘一致）
    'min_turnover': 3000,    # 最小成交额：3000万元（API返回单位为万元，放宽，实盘5000万）
    'momentum_days': 20,         # 动量计算天数
    'min_price': 1.0,            # 最小股价
    'max_stocks': 3,             # 推荐数量：3只（与实盘一致）
    'min_strength_score': 50     # 强势分数要求：50（与实盘一致）
}

# 回测采样配置
BACKTEST_SAMPLE_CONFIG = {
    'sample_size': 300,          # 采样数量：300只（全部沪深300成分股）
    'random_seed': 42,           # 固定随机种子，确保可重复性
    'use_cache': True,           # 启用缓存加速
    'cache_expire_days': 7       # 缓存过期天数
}

# 回测输出配置
BACKTEST_OUTPUT_CONFIG = {
    'save_json': True,           # 保存JSON结果
    'save_report': True,         # 自动生成Markdown报告
    'verbose': True              # 详细日志输出
}

# 对比：实盘配置 vs 回测配置
"""
                    实盘配置        回测配置        说明
PE上限              30             30            ✅ 与实盘一致
成交额              5000万         3000万        ⚠️ 放宽，增加样本量
推荐数量            3只            3只           ✅ 与实盘一致
强势分数            50             50            ✅ 与实盘一致
采样数量            300只          300只         ✅ 全部沪深300成分股

配置说明：
1. 改用成交额筛选（更准确反映流动性）
2. 除成交额外，所有参数与实盘保持一致
3. 成交额放宽至3000万，以获得更多历史数据样本
4. 使用全部300只成分股，不再采样
5. 回测结果更接近实盘表现
"""
